%h2= @company.name

%h3
  Упоминания (#{@company.company_mentions.count})
  %ul
    - @company.company_mentions.each do |cm|
      %li= link_to cm, cm
      
%h3
  Статьи (#{@company.articles.count})
  %ul
    - @company.articles.each do |a|
      %li= link_to a

= render :partial => 'graphic'
